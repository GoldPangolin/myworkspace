import { Injectable } from "@angular/core";
import { NavigationItem } from "./navigation.types";

/** this will be a store like service for my navigation */
@Injectable({ providedIn: 'root' })
export class FuseNavigationService {
    
}