import { PerfectScrollDirective } from './perfect-scroll.directive';

describe('PerfectScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new PerfectScrollDirective();
    expect(directive).toBeTruthy();
  });
});
